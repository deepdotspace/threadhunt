/*
 * New topic (/new-topic): the two-question setup flow. The screen is a single
 * centered scrolling column; all logic lives in the NewTopicForm component.
 */

import { NewTopicForm } from '../../components/newtopic'

export default function NewTopicPage() {
  return <NewTopicForm />
}
